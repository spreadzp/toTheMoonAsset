import { Command } from '@oclif/command';
export default class Compile extends Command {
    static description: string;
    static args: {
        name: string;
        description: string;
        required: boolean;
    }[];
    static flags: {
        fullInfo: import("@oclif/parser/lib/flags").IBooleanFlag<boolean>;
    };
    private compile;
    run(): Promise<void>;
}
