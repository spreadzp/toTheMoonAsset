import Mocha from 'mocha';
export declare class TestRunner {
    private mocha;
    private static instance;
    constructor(mochaOptions: Mocha.MochaOptions);
    static getInstance(): TestRunner;
    getContractFile: (fileNameOrPath: string) => string;
    addFile(path: string): void;
    run({ envName, verbose }: {
        envName?: string;
        verbose: boolean;
    }): Promise<void>;
    checkNode(nodeUrl?: string): Promise<void>;
}
export default TestRunner;
